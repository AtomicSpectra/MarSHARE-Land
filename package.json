{
  "name": "marshare-land",
  "version": "1.0.0",
  "description": "Root package to manage frontend and backend concurrently",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix my-project",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix my-project",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
